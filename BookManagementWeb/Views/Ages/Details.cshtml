@model BookManagementLib.BusinessObject.Age

@{
    ViewData["Title"] = "Details";
    Layout = "~/Views/Shared/_Layout"+TempData.Peek("role")+".cshtml";
}

<h1>Details</h1>

<div>
    <h4>Age</h4>
    <hr />
    <dl class="row">
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.ForAgesId)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.ForAgesId)
        </dd>
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.Description)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.Description)
        </dd>
    </dl>
</div>
<div>
    @Html.ActionLink("Edit", "Edit", new { id = Model.ForAgesId }) |
    <a asp-action="Index">Back to List</a>
</div>
@{
    if (ViewBag.Message != null)
    {
        <script>
        window.onload = function () { alert("@ViewBag.Message") }
        </script>
    }
}